function sum(a, b) {
    return a + b;
}

function sub(a, b){
    return a - b;
}

function times(a, b){
    return a * b;
}

function pow(a, b){
    return Math.pow(a, b);
}

module.exports = {
    sum, sub, times, pow
}
